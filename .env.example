# Use a random value of at least 32 bytes. Never commit the real secret.
JWT_SECRET=replace-with-a-random-secret-at-least-32-bytes
JWT_EXPIRATION=86400000
GATEWAY_CORS_ALLOWED_ORIGIN=http://localhost:3000
